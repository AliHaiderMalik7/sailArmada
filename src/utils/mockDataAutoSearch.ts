// utils/mockData.ts

// Define the type for the mock data entries
export interface SearchOption {
  id: number;
  name: string;
  countryName: string;   // Add countryName to the interface
  imageUrl: string;
  yachtCount: number;
  city: string;
}

  